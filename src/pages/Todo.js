import React from 'react'


const Todo = () => (
  <div style={{flex: "1"}}>
    <h1>
      add note to double
      check schedule times, add helper instructions for batch upload
    </h1>
  </div>
);

export default Todo